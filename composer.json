{
    "name": "event-loader/task",
    "description": "Event loading mechanism task",
    "type": "project", 
    
    "require": {
        "symfony/lock": "^7.2",
        "psr/log": "^3.0",
        "php": ">=8.1"
    },

    "autoload": {
        "psr-4": {
            "EventLoadingSystemDesign\\": "./"
        }
    },
    "require-dev": {
        "phpunit/phpunit": "^12.1"
    }
}
